#ifndef URLAND_SPRITE_COMPONENT_H
#define URLAND_SPRITE_COMPONENT_H

struct Sprite {
    // index of the first frame
    int index;
    // Width of sprites in tiles
    int width;
    // Height of sprite in tiles
    int height;
};

#endif // URLAND_SPRITE_COMPONENT_H